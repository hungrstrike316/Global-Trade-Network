function PIF_VonMises(x)

disp(['Von Mises PIF: Not sure this makes sense to do. PIF is only positive and therefore',...'
    'can not delay Oscillator i if it is leading Oscillator j. Not sure the',...
    '2 oscillators will ever be guaranteed to sync.'])